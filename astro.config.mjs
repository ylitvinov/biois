// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://ylitvinov.github.io',
  base: '/biois',
  // Для кастомного домена: убрать base и поменять site
  // site: 'https://biois.ru',
});
